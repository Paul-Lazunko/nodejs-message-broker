export enum EMessageStatus {
  DEFAULT = 'enqueued',
  RECEIVED = 'received',
  DELIVERED = 'delivered',
  HANDLED = 'handled',
  NOT_DELIVERED = 'notDelivered',
  NOT_HANDLED = 'notHandled',
  EXPIRED = 'expired',
}
